<!DOCTYPE html>
<html>
  <head>
    <title>Socket.IO chat</title>
    <style>
      
    </style>
  </head>
  <body>
    {{!-- <ul id="messages">
      <li>Waiting for host to start game</li>
    </ul> --}}
    {{!-- <form id="form" action="">
      <input id="input" autocomplete="off" /><button>Send</button>
    </form> --}}

    <div class='container h-100' style='background-color: rgb(234, 234, 234);'>
      <p id="username"></p>

      {{!-- question --}}
      <div class="container w-50">
        <div class="col p-5 m-5" style="border: 2px solid black; background-color: white;">
          <p class="text-center fs-1" style="font-family: monaco;">What color is the sky?</p>
        </div>
      </div>

      {{!-- answer choices --}}
      <div class="container py-5">
        <div class="row row-cols-2">
          <button class="btn col p-3 text-center text-white fs-4" style="font-family: monaco; background-color: #E11B3C;">Blue</button>
          <button class="btn col p-3 text-center text-white fs-4" style="font-family: monaco; background-color: #1A68CE;">Pink</button>
          <button class="btn col p-3 text-center text-white fs-4" style="font-family: monaco; background-color: #D79D00;">Yelllow</button>
          <button class="btn col p-3 text-center text-white fs-4" style="font-family: monaco; background-color: #26890C;">Purple</button>
        </div>
      </div>

    </div>
    
  </body>
</html>
<script src="/socket.io/socket.io.js"></script>
<script>
  const socket = io();
  var form = document.getElementById('form');
  var input = document.getElementById('input');
  const username = sessionStorage.getItem("username");
  document.getElementById('username').innerText = `Current User: ${username}`;
  console.log(username);
  console.log('attempting to emit username');
  socket.emit('connect user', username);
  socket.on('connect user', function(id) {
    console.log('id:', id);
  });
</script>